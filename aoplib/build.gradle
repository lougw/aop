apply plugin: 'com.android.library'
apply plugin: 'com.novoda.bintray-release'
apply plugin: 'maven'

android {
    compileSdkVersion 28
    defaultConfig {
        minSdkVersion 3
    }
}

dependencies {
    implementation 'org.aspectj:aspectjrt:1.8.9'

}
//uploadArchives {
//    repositories {
//        mavenDeployer {
//            //设置插件的GAV参数
//            pom.groupId = 'com.lougw.aop'
//            pom.artifactId = 'buildaop'
//            pom.version = 1.0
//            //文件发布到下面目录
//            repository(url: uri('../release'))
//        }
//    }
//}
publish {
    userOrg = 'gaoweilou' //bintray注册的用户名（不是邮箱）
    groupId = 'com.lougw.aop'
    artifactId = 'buildaop' //项目名称
    publishVersion = '4.0' //版本号
    desc = '' //项目描述。可空。
    website = '' //项目地址，一般填github地址。可空。
}